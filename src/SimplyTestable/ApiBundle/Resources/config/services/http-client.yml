services:
  SimplyTestable\ApiBundle\Services\FooHttpClientService:
    arguments:
      - '%curl_options%'

  GuzzleHttp\Client:
    factory: 'SimplyTestable\ApiBundle\Services\FooHttpClientService:getHttpClient'
